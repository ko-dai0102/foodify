<% if user_signed_in? %>
  <%= turbo_frame_tag user do %>
    <% if current_user == user %>
    <% elsif user.followed_by?(current_user) %>
      <%=link_to "フォロー中", list_destroy_user_relationships_path(user.id), class: "unfollow-btn", data: { turbo_method: :delete } %>
    <% else %>
      <%=link_to "フォローする", list_create_user_relationships_path(user.id), class: "follow-btn", data: { turbo_method: :post } %>
    <% end %>
  <% end %>
<% else %>
    <%=link_to "フォローする", new_user_session_path, class: "follow" %>
<% end %>